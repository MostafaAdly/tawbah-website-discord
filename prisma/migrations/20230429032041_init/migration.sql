/*
  Warnings:

  - You are about to drop the column `connected_guilds` on the `audio` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `audio` DROP COLUMN `connected_guilds`;
